1.main.py
  檔案內包含的實作方法有下列幾個：
  A. XGBoots
  B. Random Forest
  C. Rule Based
  D. XGBoots + Rule Based

2. Character.py 及 RuleBasedEntity_Class.py
  則是實作RuleBased(Entity_class)的方法。

因為XGBoots及Random Forest的方法每次跑出來的結果，
有時候會不相同，因此有放result的圖片檔。